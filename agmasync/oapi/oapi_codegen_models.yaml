package: oapi
generate:
  models: true
output: agmasync/oapi/models.gen.go
output-options:
  # Optional attributes are nullable: `null` on a write removes one, so the
  # models must tell unset, null, and a value apart.
  nullable-type: true
